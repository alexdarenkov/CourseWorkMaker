# AGENTS.md — CourseWorkMaker (md2docx)

Файл-указатель для AI-агентов. Вся документация проекта живёт в `docs/`
(spec-driven development); этот файл намеренно короткий — не дублируй
содержимое сюда, правь первоисточники.

**CourseWorkMaker** — веб-приложение для написания и оформления курсовых
работ по ГОСТ 7.32-2017: Markdown-редактор, живое постраничное превью
«как в Word», серверный экспорт в DOCX/PDF, ИИ-генерация. Документация,
комментарии и UI — на русском.

## Куда смотреть

| Вопрос | Документ |
|--------|----------|
| Процесс работы с документацией (SDD) | `docs/README.md` |
| Что система должна делать (поведение, источник истины) | `docs/specs/` |
| Как система устроена: сервисы, порты, стек, маршруты | `docs/architecture/overview.md` |
| API-эндпоинты, переменные окружения | `docs/architecture/api.md` |
| Схемы ключевых потоков (экспорт, ИИ-генерация, auth) | `docs/architecture/flows/` |
| Стиль кода (TS/Java/Python) | `docs/architecture/code-style.md` |
| Почему принято то или иное решение | `docs/decisions/` |
| Команды сборки/тестов, главные инварианты | `CLAUDE.md` |
| Пользовательская документация, быстрый старт | `README.md` |
| Текст стандарта | `ГОСТ 7.32-2017.pdf` |

## Главные правила

1. Меняя поведение — правь спеку в `docs/specs/` в том же изменении
   (процесс — `docs/README.md`).
2. Превью и DOCX — два параллельных рендера: правки диалекта/оформления
   вносятся на ОБЕ стороны (`frontend/src/lib/` ↔ `services/converter/app/`)
   с тестами обеих сторон.
3. Стиль кода: минимальные изменения, следуй стилю файла; комментарии
   и сообщения об ошибках — на русском (`docs/architecture/code-style.md`).
4. Существующую логику тестов не менять — исправлять только поломки
   от изменения интерфейсов.
