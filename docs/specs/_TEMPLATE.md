# Спека: <область поведения>

<Один абзац: что покрывает спека, где реализовано. Для правил рендера —
напоминание об инварианте двух сторон. Ссылка на схему в
../architecture/flows/, если есть.>

Префикс ID: `<XX>`. Статусы пунктов: `as-built` | `принят` | `реализован` | `отменён (ADR-NNNN)`.

## XX-1. <Название правила> — `<статус>`

**Зачем** (необязательный слой — только если мотивация неочевидна):
<бизнес-причина: чьё требование, какую боль решает, что случится без этого.>

<Требование: формулировка поведения, включая граничные случаи. Пишется так,
чтобы по ней можно было написать тест.>

**Критерии приёмки** (необязательный слой — где формулировки мало):
```gherkin
Сценарий: <название>
  Дано <исходное состояние>
  Когда <действие>
  Тогда <проверяемый результат>
    И <для правил рендера: на ОБЕИХ сторонах — превью и DOCX>
```

- Реализация: <файл(ы); для рендера — обе стороны: `frontend/...` ↔ `services/converter/...`>
- Тесты: <файл::тест — имя теста должно содержать ID пункта | «нет — дыра покрытия»>
- Решения: <ADR-NNNN | —>
