/**
 * Геометрия ленты превью в пикселях (96 dpi, до применения зума). Одни и те же
 * числа нужны вёрстке страниц (PreviewPane), авто-подгонке масштаба (useZoom) и
 * синхронной прокрутке (scrollSync) — поэтому живут в одном месте: разъедутся
 * они — превью прокрутится не туда.
 */

/** Ширина страницы А4 (210 мм). */
export const PAGE_WIDTH_PX = 794

/** Высота страницы А4 (297 мм). */
export const PAGE_HEIGHT_PX = 1123

/** Зазор между листами в ленте (margin-bottom страницы). */
export const PAGE_GAP_PX = 30

/** Верхнее поле листа (20 мм) — с него начинается контент страницы. */
export const PAGE_PAD_TOP_PX = (20 / 25.4) * 96

/** Левое поле листа по ГОСТ (30 мм). */
export const PAGE_PAD_LEFT_PX = (30 / 25.4) * 96

/** Правое поле листа по ГОСТ (15 мм). */
export const PAGE_PAD_RIGHT_PX = (15 / 25.4) * 96

/** Отступ ленты от верха панели превью (класс pt-7 = 1.75rem). */
export const PREVIEW_PAD_TOP_PX = 28
